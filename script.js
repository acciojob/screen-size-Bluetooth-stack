//your JS code here. If required.
 function getSize(){
      let div = document.querySelector('div>h1');
      let h = window.innerHeight;
      let w = window.innerWidth;
      
      div.textContent = "Width: " + w + " and Height: " + h
      // alert("loded");
    }
